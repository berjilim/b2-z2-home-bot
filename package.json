{
  "name": "beza-home-bot",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Standalone always-on home guardian (Claude + ACP) for HA Supervisor",
  "scripts": {
    "start": "node src/main.mjs",
    "spike": "node spike/acp-roundtrip.mjs",
    "runner-check": "node spike/runner-check.mjs",
    "test": "node src/orders.test.mjs && node src/triggers.test.mjs && node src/listener.test.mjs && node src/wake-glue.test.mjs && node src/telegram-bot.test.mjs && node src/agent-runner.test.mjs"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "^0.42.0",
    "dotenv": "^17.4.2",
    "hass-mcp": "^0.1.7",
    "ws": "^8.21.0"
  }
}
